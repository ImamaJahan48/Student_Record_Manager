# Student_Record_Manager
Student_Record_Manager is a Python-based application that allows users to efficiently create, update, search, and manage student records using a structured data system. The project demonstrates file handling, data structures, and basic CRUD operations.
